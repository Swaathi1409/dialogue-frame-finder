# Core video acquisition
yt-dlp>=2024.1.1

# Frame extraction and video processing
opencv-python-headless>=4.9.0
numpy>=1.24.0,<2.0.0

# Bundled ffmpeg binary (so ffmpeg does not need to be on system PATH)
# imageio-ffmpeg ships the actual ffmpeg executable for the current platform.
imageio-ffmpeg>=0.4.9

# Fallback for ok.ru TLS fingerprint blocking
playwright>=1.46.0

# ASR - coarse temporal localization
faster-whisper>=1.0.0

# OCR - visual text detection (primary engine)
# Pinned to 2.7.x: PaddleOCR 3.x has breaking API changes and crashes on
# Windows CPU due to a PIR executor + oneDNN incompatibility in paddlepaddle 3.x.
# 2.7.3 + 2.6.2 is the stable, tested combination on CPU-only environments.
paddleocr==2.7.3
paddlepaddle==2.6.2

# Text matching
rapidfuzz>=3.6.0

# CLI and utilities
# (argparse is in the standard library, no extra install needed)

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Web UI
flask>=3.0.0
